name: C++ CI/CD

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install build tools
        run: sudo apt update && sudo apt install -y g++ make

      - name: Compile
        run: g++ -o app main.cpp

      - name: Run
        run: ./app

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile release binary
        run: g++ -O2 -o app main.cpp

      - name: Deploy (placeholder)
        run: echo "Add your deploy steps here (e.g. scp to server, docker push, etc.)"name: C++ CI/CD

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install build tools
        run: sudo apt update && sudo apt install -y g++ make

      - name: Compile
        run: g++ -o app main.cpp

      - name: Run
        run: ./app

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile release binary
        run: g++ -O2 -o app main.cpp

      - name: Deploy (placeholder)
        run: echo "Add your deploy steps here (e.g. scp to server, docker push, etc.)"
